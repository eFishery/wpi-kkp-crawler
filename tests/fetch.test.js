const fetch = require('../libs/fetch');

test('fetch url', () => {
  // expect.assertions(1);
  // return fetch({
  //   start: '2017-02-01',
  // }).then((data) => {
  //   expect(data).toHaveProperty('2017-02-01');
  // });
  expect(1 + 2).toBe(3);
});
